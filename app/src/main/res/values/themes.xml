<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Completecalc" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Mantenha configurações essenciais -->
        <item name="colorPrimary">@color/cinza_escuro</item>
        <item name="colorSecondary">@color/cinza_claro</item>
        <item name="colorSurface">@color/verde_escuro</item> <!-- Cor do botão -->
        <item name="colorOnSurface">@color/white</item> <!-- Cor do texto no botão -->
        <!-- Adicione outras personalizações necessárias -->
    </style>

    <style name="Theme.Completecalc" parent="Base.Theme.Completecalc" />

</resources>